# Student_Control_System
🎓 Student Control System - A Python console application for student academic management. It records names, sections, and grades per subject, provides a ranking of the top 3 GPAs, detects failed subjects, and allows data export and import in CSV format. It includes input validation, duplicate detection, and an interactive menu interface.
